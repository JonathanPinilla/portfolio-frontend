import HeaderButton from "./HeaderButton";

const Header = () => {
  return (
    <header>
      <HeaderButton text="Home"/>
      <HeaderButton text="About me"/>
      <HeaderButton text="My projects"/>
      <HeaderButton text="Contact"/>
    </header>
  )
}

export default Header;
